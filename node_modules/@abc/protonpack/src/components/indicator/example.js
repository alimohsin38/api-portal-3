import React, {Component} from 'react'
import {Row, Col, Indicator} from '../../export'
import {iconMap} from './index'
import './example.scss'

export default class IndicatorExample extends Component {
    handleClick(indicatorType) {
        return console.log(indicatorType) // eslint-disable-line no-console
    }

    render() {
        return (
            <Row>
                <Col>
                    <h2 className="mb-2">Indicator</h2>

                    <h4 className="mb-2">Types</h4>
                    <Row className='mb-3' style={{backgroundColor: 'white', whiteSpace: 'nowrap'}}>
                        {Object.keys(iconMap).map(key => <Col key={key}><Indicator type={key} /> {key}</Col>)}
                    </Row>

                    <h4 className="mb-2">Style Type</h4>
                    <Row className='mb-3' style={{backgroundColor: 'white'}}>
                        <Col><Indicator type="timer"/> - (default)</Col>
                        <Col><Indicator type="timer" styleType='primary'/> - primary</Col>
                        <Col><Indicator type="timer" styleType='info'/> - info</Col>
                        <Col><Indicator type="timer" styleType="warning"/> - warning</Col>
                        <Col><Indicator type="timer" styleType="danger"/> - danger</Col>
                        <Col><Indicator type="timer" styleType="success"/> - success</Col>
                        <Col><Indicator type="notes" disabled/> - disabled</Col>
                    </Row>

                    <h4 className="mb-2">Sizes</h4>
                    <Row className='mb-3' style={{backgroundColor: 'white'}}>
                        <Col><Indicator type="select-date"/> - (default)</Col>
                        <Col><Indicator type="select-date" size='xs'/> - xs</Col>
                        <Col><Indicator type="select-date" size='sm'/> - sm</Col>
                        <Col><Indicator type="select-date" size="md"/> - md</Col>
                        <Col><Indicator type="select-date" size="lg"/> - lg</Col>
                        <Col><Indicator type="select-date" size="xl"/> - xl</Col>
                        <Col><Indicator type="select-date" size="xxl"/> - xxl</Col>
                    </Row>
                </Col>
            </Row>
        )
    }
}
