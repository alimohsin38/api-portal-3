import React from 'react'

const ToggleSwitch = (props) => {
    // eslint-disable-next-line no-unused-vars, react/prop-types
    const {normalize, validate, onValidationFieldInitialized, onValidationFieldFailure, onValidationFieldSuccess, ...remain} = props

    return (
        <label className="toggleSwitch-switch">
            <input type="checkbox" {...remain} />
            <div className="toggleSwitch-slider" />
        </label>
    )
}

export default ToggleSwitch