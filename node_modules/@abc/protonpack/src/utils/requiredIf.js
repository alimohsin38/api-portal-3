// -- https://github.com/thejameskyle/react-required-if
export default function (type, condition) {
    return function (props) {
        const test = condition(props) ? type.isRequired : type
        return test.apply(this, arguments)
    }
}