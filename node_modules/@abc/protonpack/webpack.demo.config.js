const path = require('path')
const HtmlWebpackPlugin = require('html-webpack-plugin')

module.exports = {
    bail: false,
    entry: ['./src/index.js'],
    output: {
        path: path.join(__dirname, '/dist'),
        filename: 'protonpack.[hash].js',
    },
    externals: [],
    rules: [
        {
            test: /\.css$/,
            loader: 'style-loader!css-loader!postcss-loader',
        },
        {
            test: /\.scss$/,
            loader: 'style-loader!css-loader!postcss-loader!sass-loader',
        },
        {
            test: /\.js$/,
            exclude: /node_modules/,
            loader: 'babel-loader',
            query: {presets: ['es2015', 'react']},
        },
    ],
    devServer: {
        contentBase: './src',
        port: 4242,
        progress: true,
        inline: true,
    },
    plugins: [
        new HtmlWebpackPlugin({template: './src/export.html'}),
    ],
    devtool: 'source-map',
}
