import {validateUrl} from '../../export'

describe('validateUrl', () => {
    it('should return true for a valid url', () => {
        expect(validateUrl('validurl.com')).toBe(true)
    })
    it('should return false for an invalid url', () => {
        expect(validateUrl('fake-url')).toBe(false)
    })
})
