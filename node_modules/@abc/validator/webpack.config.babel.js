import path from 'path'

const libraryName = 'validator'
const outputFile = `${libraryName}.js`

const plugins = []

export default {
    bail: true,
    entry: ['./src/export.js'],
    devtool: 'source-map',
    output: {
        path: path.join(__dirname, '/dist'),
        filename: outputFile,
        library: libraryName,
        libraryTarget: 'umd',
        umdNamedDefine: true,
    },
    module: {
        loaders: [{
            test: /(\.js)$/,
            loader: 'babel-loader',
            exclude: /node_modules/,
        }],
    },
    plugins,
    resolve: {
        alias: {
            mask: `${path.resolve('./src')}/mask`,
            validation: `${path.resolve('./src')}/validation`,
        },
    },
}
