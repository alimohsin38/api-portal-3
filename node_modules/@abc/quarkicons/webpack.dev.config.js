const path = require('path')

module.exports = {
    entry: ['./src/'],
    output: {
        path: path.join(__dirname, '/dist'),
        filename: 'quarkicons.js',
        library: 'quarkicons',
        libraryTarget: 'umd'
    },
    externals: [],
    devServer: {
        contentBase: './src',
        port: 4244,
        progress: true,
        colors: true,
        inline: true
    },
    devtool: 'source-map'
}