const path = require('path')

module.exports = {
    bail: false,
    entry: ['./src'],
    output: {
        path: path.join(__dirname, 'dist'),
        filename: 'fedagents.js',
        library: 'fedagents',
        libraryTarget: 'umd',
    },
    externals: [],
    devServer: {
        contentBase: './src',
        port: 4243,
        progress: true,
        colors: true,
        inline: true,
    },
    devtool: 'source-map',
    plugins: [],
}