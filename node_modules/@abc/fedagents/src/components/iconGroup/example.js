import React, {Component} from 'react'
import PropTypes from 'prop-types'
import {Row, Col} from '@abc/protonpack'
import {IconGroup} from '../../export'

export default class IconGroupExample extends Component {
    render() {
        return (
            <div className={this.props.className}>
                <h2>Icon Group -- DEPRECATED</h2>
                <Row>
                    <Col>
                        <IconGroup type="phone">Icon Group</IconGroup>
                    </Col>
                    <Col>
                        <IconGroup type="gear">Second Group</IconGroup>
                    </Col>
                    <Col>
                        <IconGroup type="user">Some other group</IconGroup>
                    </Col>
                </Row>
            </div>
        )
    }
}

IconGroupExample.propTypes = {
    className: PropTypes.string,
}