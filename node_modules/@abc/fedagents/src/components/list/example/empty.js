import React, {Component} from 'react'
import {List} from '../../../export'

export default class EmptyListExample extends Component {
    render() {
        return (
            <List
                displayData={[]}
                item={() => {}}
                {...this.props}
            />
        )
    }
}
