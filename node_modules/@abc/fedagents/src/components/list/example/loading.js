import React, {Component} from 'react'
import {List} from '../../../export'

export default class LoadingListExample extends Component {
    render() {
        return (
            <List
                displayData={[]}
                item={() => {}}
                loading
                {...this.props}
            />
        )
    }
}
