import React from 'react'
import PropTypes from 'prop-types'
import {Col} from '@abc/protonpack'

const ListItem = ({children, ...remain}) => (
    <Col {...remain}>
        {children}
    </Col>
)

ListItem.propTypes = {
    children: PropTypes.any,
}

export default ListItem
