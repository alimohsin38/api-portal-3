import React, {Component} from 'react'
import PropTypes from 'prop-types'
import {Row, Col} from '@abc/protonpack'
import {HoverCard} from '../../../export'

export default class HoverCardExample extends Component {
    handleIconClick() {
        alert('You\'re missing the point Morty. Why would he drive a smaller toaster with wheels? I mean, does your car look like a smaller version of your house? No.') // eslint-disable-line no-alert
    }

    render() {
        return (
            <div className={this.props.className}>
                <h3>Hover Card</h3>
                <Row>
                    <Col>
                        <HoverCard icon="pencil" onIconClick={this.handleIconClick}>
                            <div>
                                <p>This is a test for the basic HoverCard.</p>
                                <p>Click the edit icon and recieve an alert.</p>
                            </div>
                        </HoverCard>
                    </Col>
                    <Col>
                        <HoverCard icon="pencil" isHovered onIconClick={this.handleIconClick}>
                            <div>
                                <p>This is a test for the basic HoverCard that is pre-Hovered.</p>
                                <p>Click the edit icon and recieve an alert.</p>
                            </div>
                        </HoverCard>
                    </Col>
                </Row>
            </div>            
        )
    }
}

HoverCardExample.propTypes = {
    className: PropTypes.string,
}