import React, {Component} from 'react'
import PropTypes from 'prop-types'
import OptionsCardExample from './optionsCard/example'
import HoverCardExample from './hoverCard/example'

export default class CardsExample extends Component {
    render() {
        return (
            <div className={this.props.className}>
                <h2>Cards</h2>
                <OptionsCardExample />
                <HoverCardExample />
            </div>
        )
    }
}

CardsExample.propTypes = {
    className: PropTypes.string,
}