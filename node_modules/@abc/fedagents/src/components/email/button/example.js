import React, {Component} from 'react'
import PropTypes from 'prop-types'
import {EmailButton} from '../../../export'

export default class EmailButtonExample extends Component {
    render() {
        return (
            <div className="exampleSection">
                <h3>Button</h3>
                <EmailButton displayData="zoidberg@planetexpress.com" />
            </div>  
        )
    }
}

EmailButtonExample.propTypes = {
    className: PropTypes.string,
}