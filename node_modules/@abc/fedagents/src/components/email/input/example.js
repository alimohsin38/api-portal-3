import React, {Component} from 'react'
import PropTypes from 'prop-types'
import {EmailInput} from '../../../export'

export default class EmailInputExample extends Component {
    render() {
        return (
            <div className="exampleSection">
                <h3>Input</h3>
                <EmailInput />
            </div> 
        )
    }
}

EmailInputExample.propTypes = {
    className: PropTypes.string,
}