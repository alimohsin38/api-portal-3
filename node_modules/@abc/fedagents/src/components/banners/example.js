import React, {Component} from 'react'
import PropTypes from 'prop-types'
import ModalBannerExample from './modalBanner/example'

export default class BannersExample extends Component {
    render() {
        return (
            <div className={this.props.className}>
                <h2>Banners</h2>
                <ModalBannerExample />
            </div>
        )
    }
}

BannersExample.propTypes = {
    className: PropTypes.string,
}