import React, {Component} from 'react'
import PropTypes from 'prop-types'
import {Row, Col} from '@abc/protonpack'
import {IconGroupLink} from '../../export'

export default class IconGroupExample extends Component {
    render() {
        return (
            <div className={this.props.className}>
                <h2>Icon Group Link</h2>
                <Row>
                    <Col>
                        <IconGroupLink
                            type="phone"
                            onClick={() => console.log('phone home')} // eslint-disable-line no-console
                        >
                            Call Me Now
                        </IconGroupLink>
                    </Col>
                    <Col>
                        <IconGroupLink
                            type="gear"
                            size="lg"
                            disabled
                            onClick={() => console.log('wont work when disabled')} // eslint-disable-line no-console
                        >
                            Settings
                        </IconGroupLink>
                    </Col>
                    <Col>
                        <IconGroupLink
                            type="user"
                            size="md"
                            onClick={() => console.log('handle your routing here')} // eslint-disable-line no-console
                        >
                            Profile
                        </IconGroupLink>
                    </Col>
                </Row>
            </div>
        )
    }
}

IconGroupExample.propTypes = {
    className: PropTypes.string,
}