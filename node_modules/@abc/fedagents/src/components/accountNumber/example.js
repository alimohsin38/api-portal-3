import React, {Component} from 'react'
import PropTypes from 'prop-types'
import {AccountNumber} from '../../export'

export default class AccountNumberExample extends Component {
    render() {
        return (
            <div className={this.props.className}>
                <h2>Account Number</h2>
                <div>
                    Formats and displays an account number: <AccountNumber displayData="012345" />
                </div>
            </div>
        )
    }
}

AccountNumberExample.propTypes = {
    className: PropTypes.string,
}