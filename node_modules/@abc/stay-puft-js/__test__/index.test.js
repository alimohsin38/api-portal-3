describe('Stay-Puft', () => {
    describe('Jest and Generators don\'t mix', () => {
        describe('will actually write tests when we refactor to async/await',() => {
            it('should pass without question', () => {
                expect(true).toBe(true)
            })
        })
    })
})